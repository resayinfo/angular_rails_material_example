#flash-messages{"ng-controller" => "FlashesController"}
  .alert.alert-dismissible{role: "alert", "ng-class" => "flashClass(flash)", 'ng-repeat'=>'flash in flashes'}
    %button.close{"ng-click" => "removeFlash(flash)", type: "button"}
      %span{"aria-hidden" => "true"} ×
      %span.sr-only Close
    {{flash.message}}